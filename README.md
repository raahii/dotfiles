## .files

### install

```shell
make init && make deploy
```

```shell
sudo sh -c "which fish >> /etc/shells"
chsh -s $(which fish)
```

after activated fish:

```shell
fisher
```

### update

```shell
make update
```

