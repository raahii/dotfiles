name: MacOS

on:
  push:

jobs:
  build:
    runs-on: macOS-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v1

      - name: Install tools
        run: make init

      - name: Deploy dotfiles
        run: make deploy
